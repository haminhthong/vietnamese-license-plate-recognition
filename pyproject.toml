[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --basetemp=./.pytest_tmp"

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]
